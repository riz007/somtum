---
"somtum": patch
---

Update GitHub Pages deployment workflow to build VitePress docs before deploying (`docs/.vitepress/dist/` is now the deploy artifact instead of raw `docs/`). Fix VitePress `base` to `/somtum/` for correct asset paths on GitHub Pages. Add docs link to README header.
